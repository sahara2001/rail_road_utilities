### Installation Guide

1. Install dependencies of c++ component:
`
sudo apt-get install libopenal1 libopenal-dev libsndfile-dev libmpg123-dev libmpg123 libfontconfig-dev

2. Install gosu using `gem install gosu`
`